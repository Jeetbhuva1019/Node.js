<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<style>
    body {
        font-family: 'Arial', sans-serif;
        margin: 0;
        padding: 0;
        background-color: #f9f9f9;
        color: #333;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        min-height: 100vh;
    }

    h1 {
        color: #4CAF50;
        margin-bottom: 20px;
        font-size: 2.5rem;
        text-align: center;
    }

    form {
        background: #fff;
        padding: 20px;
        border-radius: 8px;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        margin-bottom: 30px;
        width: 100%;
        max-width: 400px;
    }

    form input,
    form button {
        width: 100%;
        padding: 10px;
        margin: 10px 0;
        border: 1px solid #ddd;
        border-radius: 4px;
        font-size: 1rem;
    }

    form input:focus {
        border-color: #4CAF50;
        outline: none;
        box-shadow: 0 0 5px rgba(76, 175, 80, 0.5);
    }

    form button {
        background-color: #4CAF50;
        color: #fff;
        border: none;
        cursor: pointer;
        transition: background 0.3s;
    }

    form button:hover {
        background-color: #45a049;
    }

    form button a {
        color: #fff;
        text-decoration: none;
    }

    table {
        width: 90%;
        margin: 0 auto;
        border-collapse: collapse;
        background: #fff;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        border-radius: 8px;
        overflow: hidden;
    }

    table th,
    table td {
        padding: 15px;
        text-align: left;
        border-bottom: 1px solid #ddd;
    }

    table th {
        background-color: #4CAF50;
        color: #fff;
        text-transform: uppercase;
    }

    table tr:nth-child(even) {
        background-color: #f2f2f2;
    }

    table tr:hover {
        background-color: #f1f1f1;
        transition: background 0.2s;
    }

    table button {
        padding: 8px 12px;
        border: none;
        border-radius: 4px;
        color: #fff;
        cursor: pointer;
        font-size: 0.9rem;
        transition: background 0.3s;
    }

    table button a {
        text-decoration: none;
        color: #fff;
    }

    table button:first-of-type {
        background-color: #2196F3;
    }

    table button:first-of-type:hover {
        background-color: #1E88E5;
    }

    table button:last-of-type {
        background-color: #f44336;
    }

    table button:last-of-type:hover {
        background-color: #e53935;
    }

    @media (max-width: 768px) {
        table {
            font-size: 0.9rem;
        }

        form {
            width: 90%;
        }
    }
</style>

<body>
    <h1>Welcome to Book Store</h1>

    <form action="/updateData" method="post">
        <input type="text" hidden name="id" value="<%=singleData.id%>">
        <input type="text" name="name" placeholder="Enter Book Name" value="<%singleData.name%>">
        <input type="text" name="auther" placeholder="Enter Book Auther Name" value="<%singleData.auther%>">
        <input type="date" name="date" placeholder="Enter Book RealeseDate" value="<%singleData.date%>">
        <input type="number" name="price" placeholder="Enter Book Price" value="<%singleData.price%>">
        <button type="submit">Update Data</button>
    </form>
</body>

</html>